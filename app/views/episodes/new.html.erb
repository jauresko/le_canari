<%= simple_form_for [@book, @episode] do |f| %>
  <%= f.input :title %>
  <%= f.input :episode_number %>
  <%= f.input :pages, as: :file, input_html: { multiple: true } %>
  <%= f.button :submit, "Submit" %>
<% end %>
